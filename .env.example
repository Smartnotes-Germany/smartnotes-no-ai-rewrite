# Public Convex URL used by the browser build and as the SSR fallback on Vercel.
# Must be your `https://<deployment>.convex.cloud` URL, not the `.convex.site` URL.
# Cannot end with a trailing "/".
VITE_CONVEX_URL="https://[deployment].convex.cloud"
